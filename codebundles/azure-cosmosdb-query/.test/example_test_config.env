# Example environment variables for testing the azure-cosmosdb-query codebundle
# After running 'task setup', use 'task show-credentials' to get your actual values

# Cosmos DB Connection (secrets)
COSMOSDB_ENDPOINT=https://rw-cosmosdb-test-XXXXX.documents.azure.com:443/
COSMOSDB_KEY=your-primary-key-here

# Database Configuration (user variables)
DATABASE_NAME=testdb
CONTAINER_NAME=testcontainer

# Example 1: Find error documents
COSMOSDB_QUERY="SELECT * FROM c WHERE c.status = 'error'"
TASK_TITLE="Find error documents in Cosmos DB"

# Example 2: Count error documents (for SLI)
# COSMOSDB_QUERY="SELECT COUNT(1) FROM c WHERE c.status = 'error'"
# TASK_TITLE="Count error documents"

# Example 3: Parameterized query
# COSMOSDB_QUERY="SELECT * FROM c WHERE c.status = @status"
# QUERY_PARAMETERS='{"@status": "error"}'
# TASK_TITLE="Find documents by status parameter"

# Example 4: Failed orders
# COSMOSDB_QUERY="SELECT * FROM c WHERE c.failed = true"
# TASK_TITLE="Find failed orders"

# Example 5: High retry count
# COSMOSDB_QUERY="SELECT c.id, c.name, c.retryCount FROM c WHERE c.retryCount > 5"
# TASK_TITLE="Find orders with high retry count"

# Optional: Query parameters (JSON string)
# QUERY_PARAMETERS=

# How to use:
# 1. Run 'task setup' to create test infrastructure
# 2. Run 'task show-credentials' to get endpoint and key
# 3. Update COSMOSDB_ENDPOINT and COSMOSDB_KEY above
# 4. Source this file: source .test/example_test_config.env
# 5. Run: robot runbook.robot

