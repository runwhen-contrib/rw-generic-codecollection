name: Score CodeCollection
on: 
  workflow_dispatch:

jobs:
  score-codebundles:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: runwhen-contrib/github-actions/codecollection-score@tree/score
        with:
          directory: './codebundles'
